const app = document.querySelector('#app'); let state;
const text = (v = '') => String(v).replace(/[&<>'"]/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' }[c]));
function render() { const step = state.runbook.steps[Math.max(0, Math.min(state.currentStep, state.runbook.steps.length - 1))]; app.innerHTML = `<section class="notes"><p class="eyebrow">PRESENTER NOTES</p><p data-demo-current-step="${text(step.id)}">Step ${Math.max(1, state.currentStep + 1)} of ${state.runbook.steps.length} — ${text(step.title)}</p><h1>${text(step.objective)}</h1><h2>Say</h2><p>${text(step.say)}</p><h2>Do</h2><p>${text(step.action)}</p>${step.prompt ? `<h2>Prompt</h2><pre>${text(step.prompt)}</pre><button data-demo-control="copy">Copy prompt</button>` : ''}<h2>Expected result</h2><p>${text(step.expected_result)}</p><h2>Audience should notice</h2><p>${text(step.audience_takeaway)}</p><p class="muted">Fallback: ${text(step.fallback)}</p><footer><button data-demo-control="previous">Previous</button><button data-demo-control="next">Next</button></footer></section>`; document.querySelector('[data-demo-control="previous"]')?.addEventListener('click', () => window.demo.previous()); document.querySelector('[data-demo-control="next"]')?.addEventListener('click', () => window.demo.next()); document.querySelector('[data-demo-control="copy"]')?.addEventListener('click', () => window.demo.copyPrompt(step.prompt)); }
window.demo.onState((next) => { state = next; render(); }); window.demo.getState().then((next) => { state = next; render(); });
